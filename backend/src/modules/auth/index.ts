import authRoutes from '../../routes/authRoutes';
export * as authController from '../../controllers/authController';
export * as authValidator from '../../validators/authValidator';

export { authRoutes };
export default authRoutes;
